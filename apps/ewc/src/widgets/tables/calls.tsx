/**
 * Calls table Widget — рендер через Shape pattern.
 * Shape отвечает за data + columns + sorting; Widget — точка композиции.
 */
const Calls = Widget(() => <Shapes.CallsTable />);

export default Calls;
