{
  "name": "@capsuletech/agent",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/agent/src",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node ../../packages/cli/bin/capsule.mjs",
        "cwd": "apps/agent",
        "interactive": true,
        "parallel": false
      }
    },
    "serve": {
      "command": "pnpm capsule dev",
      "options": {
        "cwd": "apps/agent"
      },
      "dependsOn": ["^build"]
    },
    "build": {
      "command": "pnpm capsule build",
      "options": {
        "cwd": "apps/agent"
      },
      "dependsOn": ["^build"]
    }
  }
}
