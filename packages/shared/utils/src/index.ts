// Placeholder. Originally re-exported `./z`, which has since moved to
// `@capsuletech/shared-zod`. The package itself has no remaining consumers; keep
// the file present so `vite build` succeeds (and the publish pipeline does
// not block) until the package is either repurposed or removed.
export {};
